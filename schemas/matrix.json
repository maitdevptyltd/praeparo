{
  "$defs": {
    "MatrixFilterConfig": {
      "additionalProperties": false,
      "description": "Declarative global filter applied to a matrix query.",
      "properties": {
        "field": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Target column expressed as 'table.column'.",
          "title": "Field"
        },
        "include": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Allowed values that remain after the filter is applied.",
          "title": "Include"
        },
        "expression": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Raw DAX filter expression inserted into the evaluation context.",
          "title": "Expression"
        }
      },
      "title": "MatrixFilterConfig",
      "type": "object"
    },
    "MatrixTotals": {
      "description": "Supported total display options for a matrix visual.",
      "enum": [
        "off",
        "row",
        "column",
        "both"
      ],
      "title": "MatrixTotals",
      "type": "string"
    },
    "MatrixValueConfig": {
      "additionalProperties": false,
      "description": "Declarative configuration for a matrix value column.",
      "properties": {
        "id": {
          "description": "Unique identifier or measure name for the value column.",
          "title": "Id",
          "type": "string"
        },
        "show_as": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional calculation hint such as 'Percent of column total'.",
          "title": "Display rule"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Friendly label used in rendered visuals; defaults to the value id.",
          "title": "Label"
        },
        "format": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Formatting directive, e.g. 'percent:0' or 'duration:hms'.",
          "title": "Format"
        }
      },
      "required": [
        "id"
      ],
      "title": "MatrixValueConfig",
      "type": "object"
    },
    "RowTemplate": {
      "additionalProperties": false,
      "description": "Template configuration for a matrix row dimension column.",
      "properties": {
        "template": {
          "description": "Row template expressed using Jinja-style placeholders.",
          "title": "Template",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional override for the rendered column header.",
          "title": "Label"
        }
      },
      "required": [
        "template"
      ],
      "title": "RowTemplate",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Top-level configuration for a matrix visual.",
  "properties": {
    "type": {
      "const": "matrix",
      "description": "Visual type identifier; must be 'matrix'.",
      "title": "Type",
      "type": "string"
    },
    "title": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Matrix title used in decks or charts.",
      "title": "Title"
    },
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional helper text for authors.",
      "title": "Description"
    },
    "define": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional DAX DEFINE statements prefixed to generated queries.",
      "title": "Define"
    },
    "rows": {
      "description": "Row templates expressed using Jinja-style placeholders.",
      "items": {
        "$ref": "#/$defs/RowTemplate"
      },
      "minItems": 1,
      "title": "Rows",
      "type": "array"
    },
    "values": {
      "description": "Collection of value columns that populate the matrix body.",
      "items": {
        "$ref": "#/$defs/MatrixValueConfig"
      },
      "minItems": 1,
      "title": "Values",
      "type": "array"
    },
    "totals": {
      "$ref": "#/$defs/MatrixTotals",
      "default": "off",
      "description": "Controls whether grand totals are displayed for rows, columns, or both."
    },
    "filters": {
      "description": "Global filters applied to every generated query before evaluation.",
      "items": {
        "$ref": "#/$defs/MatrixFilterConfig"
      },
      "title": "Filters",
      "type": "array"
    }
  },
  "required": [
    "type",
    "rows",
    "values"
  ],
  "title": "MatrixConfig",
  "type": "object"
}