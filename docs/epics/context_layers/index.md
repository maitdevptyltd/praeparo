# Context Layers Epics

This family tracks framework work around layered context files, repeatable
`--context` semantics, pack-shaped context payloads, and named override
behavior for `calculate`, `define`, and `filters`.

Use these phase records as implementation history. For the current supported
contract, start with [Projects / Context Layers](../../projects/context_layers.md),
[Projects / Pack Runner](../../projects/pack_runner.md), and
[Visuals / Visual Context Models](../../visuals/visual_context_model.md).

Phases:

- [Phase 1: Context Layers](1_context_layers.md)
  Status: **Implemented**
- [Phase 2: Unified Context Templating And Named Filter Overrides](2_unified_context_templating_and_named_filter_overrides.md)
  Status: **Implemented**
