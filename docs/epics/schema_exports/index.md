# Schema Exports Epics

This family tracks framework work around schema export, umbrella-schema
generation, and downstream editor consumption.

Use these phase records as implementation history. For the current supported
contract, start with [Visuals / Visual Umbrella Schema](../../visuals/visual_umbrella_schema.md).

Phases:

- [Phase 1: Visual Umbrella Schema CLI](1_visual_umbrella_schema_cli.md)
  Status: **Implemented**
