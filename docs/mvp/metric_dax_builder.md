# Metric → DAX Builder (MVP Link)

The canonical documentation for the metric compiler now lives under the main docs tree:

- `docs/metrics/metric_dax_builder.md`

This MVP page is kept only to avoid breaking existing links.
