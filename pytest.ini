[pytest]
addopts = -q
asyncio_mode = auto
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: marks tests that hit live Power BI services (requires PRAEPARO_RUN_POWERBI_TESTS=1)
